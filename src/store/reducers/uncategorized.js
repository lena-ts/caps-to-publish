const initialState = {
        id: "uncategorized",
        title: "Uncategorized",
        items: []
}

export const uncategorizedReducer = (state = initialState, action) => {
    switch(action.type) {
        default: return state
    }
}